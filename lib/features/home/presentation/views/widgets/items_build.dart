import 'package:flutter/material.dart';

class ItemsBuildWidget extends StatelessWidget {
  const ItemsBuildWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Items Build Widget"),
    );
  }
}
